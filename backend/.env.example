# Local Development Environment Variables

# Server Configuration
PORT=3004
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=https://lad-frontend-develop-741719885039.us-central1.run.app

# Mock User ID for development
DEV_USER_ID=1

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lad_dev
DB_USER=postgres
DB_PASSWORD=your_password

# JWT Secret (for development only)
JWT_SECRET=dev_secret_key_change_in_production

# Unipile API (for LinkedIn integration)
UNIPILE_API_KEY=your_unipile_api_key
UNIPILE_API_URL=https://api.unipile.com/v1

# OpenAI API (if using AI features)
OPENAI_API_KEY=your_openai_api_key

# Email Configuration (if using email campaigns)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# Twilio Configuration (if using voice/SMS campaigns)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Feature Flags
ENABLE_LINKEDIN=true
ENABLE_EMAIL=true
ENABLE_VOICE=true
ENABLE_SMS=true
